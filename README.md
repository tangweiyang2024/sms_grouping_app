# 短信分组管理应用

一个基于Flutter开发的智能短信分组管理应用，支持通过关键字和正则表达式自动分类短信。

## 功能特性

- ✅ **智能分组**: 基于关键字和正则表达式自动将短信分类
- ✅ **多种分类**: 支持银行短信、快递短信、工作短信等多种分类
- ✅ **跨平台**: 支持Android和iOS平台
- ✅ **自动分类**: 自动识别短信内容并归类到相应分组
- ✅ **本地存储**: 所有数据存储在本地，保护隐私
- ✅ **Material Design 3**: 现代化的UI设计

## 默认分组

### 1. 银行短信 💰
- 关键字: 银行、转账、余额、账单、还款、存款、贷款、信用卡、网银
- 正则: 银行卡号、交易相关词汇

### 2. 快递短信 📦  
- 关键字: 快递、物流、取件、签收、派送、驿站、丰巢、菜鸟
- 正则: 快递单号、取件码

### 3. 工作短信 💼
- 关键字: 会议、通知、工作、项目、报告、紧急、审批、办公
- 正则: 会议时间格式

### 4. 促销短信 🏷️
- 关键字: 优惠、折扣、促销、特价、限时、抢购、秒杀、立减
- 正则: 价格和折扣格式

### 5. 服务短信 🔧
- 关键字: 验证码、密码、登录、安全、提醒、通知、激活
- 正则: 验证码格式

### 6. 其他短信 📱
- 未分类的其他短信

## 技术栈

- **框架**: Flutter 3.16.5
- **语言**: Dart
- **状态管理**: StatefulWidget
- **本地存储**: SharedPreferences
- **权限处理**: permission_handler
- **CI/CD**: GitHub Actions

## 开发环境设置

### 前置要求

- Flutter SDK >= 3.2.3
- Dart SDK >= 3.2.3
- Android Studio / Xcode
- Git

### 安装步骤

1. 克隆仓库
```bash
git clone <repository-url>
cd sms_grouping_app
```

2. 安装依赖
```bash
flutter pub get
```

3. 运行应用
```bash
# Android
flutter run -d android

# iOS  
flutter run -d ios

# Web
flutter run -d web-server
```

## 构建发布版本

### Android APK
```bash
flutter build apk --release
```

### iOS IPA
```bash
flutter build ios --release --no-codesign
```

### Web应用
```bash
flutter build web --release
```

## 权限说明

### Android权限
- `READ_SMS`: 读取短信权限
- `RECEIVE_SMS`: 接收短信权限

### iOS权限  
- 由于iOS系统限制，无法直接读取短信，本应用在iOS上使用模拟数据

## 项目结构

```
sms_grouping_app/
├── lib/
│   ├── models/           # 数据模型
│   │   ├── sms_message.dart
│   │   └── sms_group.dart
│   ├── services/         # 业务逻辑
│   │   ├── group_service.dart
│   │   └── sms_service.dart
│   ├── screens/          # 页面
│   │   ├── home_screen.dart
│   │   └── group_detail_screen.dart
│   └── main.dart         # 应用入口
├── android/              # Android平台代码
├── ios/                  # iOS平台代码
├── .github/              # GitHub Actions配置
│   └── workflows/
│       └── flutter-build.yml
└── pubspec.yaml          # 依赖配置
```

## 自定义分组规则

你可以通过修改 `GroupService` 中的 `_defaultGroups` 来添加或修改分组规则：

```dart
SmsGroup(
  id: 'custom',
  name: '自定义分组',
  description: '我的自定义分类',
  keywords: ['关键字1', '关键字2'],
  regexPatterns: [r'正则表达式1', r'正则表达式2'],
  color: '#FF5722',
)
```

## GitHub Actions CI/CD

项目配置了GitHub Actions工作流，自动构建多平台版本：

- **Android**: 自动生成APK文件
- **iOS**: 自动生成IPA文件  
- **Web**: 自动构建Web版本

构建产物会自动上传到GitHub Actions Artifacts。

## 注意事项

1. **隐私保护**: 所有短信数据仅存储在本地设备，不会上传到云端
2. **权限申请**: 首次运行需要授予短信读取权限
3. **iOS限制**: 由于iOS系统安全限制，iOS版本使用模拟数据演示功能
4. **性能优化**: 大量短信时分类处理可能需要一些时间

## 贡献指南

欢迎提交Issue和Pull Request！

1. Fork本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启Pull Request

## 许可证

本项目采用MIT许可证。详见LICENSE文件。

## 作者

Claude Code - 智能短信分组管理应用

## 致谢

- Flutter团队提供优秀的跨平台框架
- 所有开源贡献者